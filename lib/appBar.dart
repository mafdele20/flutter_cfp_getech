import 'package:flutter/material.dart';

class AppBarre extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return AppBar(title: Text('CFP-GETECH'),  backgroundColor: Colors.deepOrange, centerTitle: true);
  }
}
